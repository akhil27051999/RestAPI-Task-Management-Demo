apiVersion: v1
kind: Service
metadata:
  name: task-api-service
  namespace: task-management
  labels:
    app: task-api
    tier: backend
spec:
  selector:
    app: task-api
  ports:
  - name: http
    port: 80
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
